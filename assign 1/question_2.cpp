#include <iostream>

using namespace std;

int main()
{
	int i,j,k;
	int a,b,m;
	float x[250000];
	float y[5];
	y[0]=111;
	y[1]=12567;
	y[2]=369;
	y[3]=25;
	y[4]=3;
	float u[250000];
	int f[20];
	a=1597;
	b=0;
	m=244944;
	cout<<"For a="<<a<<", b="<<b<<", m="<<m<<":\n";
	for (k=0;k<5;k++)
	{
		x[0]=y[k];
		cout<<"\nFor x[0]="<<x[0];
		//cout<<"Random u_i for x_0="<<x[0]<<"\n";
		
		u[0]=float(x[0]/m);
		for(j=0;j<249999;j++)
		{
			//cout<<u<<" ";
			x[j+1]=((a*int(x[j]))+b)%m;
			u[j+1]=float(x[j+1]/m);
		}
		cout<<"\n";

		for(i=0;i<20;i++)
		{
			f[i]=0;
		}

		for(i=0;i<250000;i++)
		{
			if(u[i]>=0 && u[i]<0.05)
				f[0]++;
			if(u[i]>=0.05 && u[i]<0.10)
				f[1]++;
			if(u[i]>=0.10 && u[i]<0.15)
				f[2]++;
			if(u[i]>=0.15 && u[i]<0.20)
				f[3]++;
			if(u[i]>=0.20 && u[i]<0.25)
				f[4]++;
			if(u[i]>=0.25 && u[i]<0.30)
				f[5]++;
			if(u[i]>=0.30 && u[i]<0.35)
				f[6]++;
			if(u[i]>=0.35 && u[i]<0.40)
				f[7]++;
			if(u[i]>=0.40 && u[i]<0.45)
				f[8]++;
			if(u[i]>=0.45 && u[i]<0.50)
				f[9]++;
			if(u[i]>=0.50 && u[i]<0.55)
				f[10]++;
			if(u[i]>=0.55 && u[i]<0.60)
				f[11]++;
			if(u[i]>=0.60 && u[i]<0.65)
				f[12]++;
			if(u[i]>=0.65 && u[i]<0.70)
				f[13]++;
			if(u[i]>=0.70 && u[i]<0.75)
				f[14]++;
			if(u[i]>=0.75 && u[i]<0.80)
				f[15]++;
			if(u[i]>=0.80 && u[i]<0.85)
				f[16]++;
			if(u[i]>=0.85 && u[i]<0.90)
				f[17]++;
			if(u[i]>=0.90 && u[i]<0.95)
				f[18]++;
			if(u[i]>=0.95 && u[i]<=1)
				f[19]++;

		}

		for(i=0;i<20;i++)
		{
			cout<<"f["<<i<<"]="<<f[i]<<"\n";
		}
	}
}